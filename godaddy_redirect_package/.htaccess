# Lago Bravo Resort - Permanent 301 Redirect for GoDaddy

# PERMANENT REDIRECT - All traffic to GitHub Pages
Redirect 301 / https://bondliden.github.io/lagobravo-website/

# Alternative method if above doesn't work on GoDaddy
RewriteEngine On
RewriteRule ^(.*)$ https://bondliden.github.io/lagobravo-website/$1 [R=301,L]

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]