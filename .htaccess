# Lago Bravo Resort - Permanent 301 Redirect

# PERMANENT REDIRECT - All traffic to new website
Redirect 301 / https://your-destination-website.com/

# Alternative method using RewriteEngine (if above doesn't work)
# RewriteEngine On
# RewriteRule ^(.*)$ https://your-destination-website.com/$1 [R=301,L]

# Force HTTPS before redirect
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]